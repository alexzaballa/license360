/*****************************************************************************************/

DEF title = 'Change Management Pack';
@@&&fc_main_table_name. '&&is_cdb.' 'DUAL' 'DUAL'
BEGIN
  :sql_text := q'[
SELECT /*+ &&top_level_hints. */
       'To be implemented...'
  FROM dual
]';
 :sql_text_cdb := q'[
SELECT /*+ &&top_level_hints. */
       'To be implemented...'
  FROM dual
]';
END;
/
@@&&9a_pre_one. 

/*****************************************************************************************/

DEF title = 'Configuration Management Pack';
@@&&fc_main_table_name. '&&is_cdb.' 'DUAL' 'DUAL'
BEGIN
  :sql_text := q'[
SELECT /*+ &&top_level_hints. */
       'To be implemented...'
  FROM dual
]';
 :sql_text_cdb := q'[
SELECT /*+ &&top_level_hints. */
       'To be implemented...'
  FROM dual
]';
END;
/
@@&&9a_pre_one. 

/*****************************************************************************************/

DEF title = 'Data Masking and Subsetting Pack for Oracle and non-Oracle Databases';
@@&&fc_main_table_name. '&&is_cdb.' 'DUAL' 'DUAL'
BEGIN
  :sql_text := q'[
SELECT /*+ &&top_level_hints. */
       'To be implemented...'
  FROM dual
]';
 :sql_text_cdb := q'[
SELECT /*+ &&top_level_hints. */
       'To be implemented...'
  FROM dual
]';
END;
/
@@&&9a_pre_one. 

/*****************************************************************************************/

DEF title = 'Diagnostic Pack';
@@&&fc_main_table_name. '&&is_cdb.' 'DUAL' 'DUAL'
BEGIN
  :sql_text := q'[
SELECT /*+ &&top_level_hints. */
       'To be implemented...'
  FROM dual
]';
 :sql_text_cdb := q'[
SELECT /*+ &&top_level_hints. */
       'To be implemented...'
  FROM dual
]';
END;
/
@@&&9a_pre_one. 


/*****************************************************************************************/

DEF title = 'Provisioning and Patch Automation Pack';
@@&&fc_main_table_name. '&&is_cdb.' 'DUAL' 'DUAL'
BEGIN
  :sql_text := q'[
SELECT /*+ &&top_level_hints. */
       'To be implemented...'
  FROM dual
]';
 :sql_text_cdb := q'[
SELECT /*+ &&top_level_hints. */
       'To be implemented...'
  FROM dual
]';
END;
/
@@&&9a_pre_one. 


/*****************************************************************************************/

DEF skip_lch = 'Y';
DEF skip_pch = 'Y';

/*****************************************************************************************/
